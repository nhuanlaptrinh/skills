interface:
  display_name: "Unify OpenClaw Bot Workspace"
  short_description: "One bot, one agent, one shared workspace"
  default_prompt: "Use $unify-openclaw-bot-workspace to normalize a clean bot or merge legacy agents into one OpenClaw main agent and workspace."
